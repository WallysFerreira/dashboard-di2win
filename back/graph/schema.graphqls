# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Contagem {
  count: Int!
}

type Query {
  extract(user_id: Int, tipo_documento: Int, data_comeco: String, data_final: String): Contagem!
}
